services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: bazaario
      POSTGRES_USER: bazaario
      POSTGRES_PASSWORD: bazaario
    ports:
      - "5432:5432"
    volumes:
      - bazaario-postgres:/var/lib/postgresql/data

volumes:
  bazaario-postgres:
